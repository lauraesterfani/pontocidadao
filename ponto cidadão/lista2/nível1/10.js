//Implemente um programa capaz de identificar se duas variáveis são pares e negativas ao mesmo tempo;

const a=2
const b=5

if(a%2==0 && b%2==0 && a<0 && b<0){
    console.log("pares e negativas")
}

else{
    console.log("depende")
}