//Implemente um programa capaz de identificar se duas variáveis são ímpares e positivas ao mesmo tempo.

const a=5
const b=8

if(a%2!==0 && b%2!==0 && a>0 && b>0){
    console.log("são ímpares e positivas")
}

else{
    console.log('depende')
}